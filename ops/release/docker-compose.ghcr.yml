services:
  backend:
    image: ${BACKEND_IMAGE:-ghcr.io/laukdan5566/chat-backend:candidate}
    pull_policy: always
  frontend:
    image: ${FRONTEND_IMAGE:-ghcr.io/laukdan5566/chat-frontend:candidate}
    pull_policy: always
