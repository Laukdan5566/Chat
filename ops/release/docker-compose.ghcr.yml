services:
  backend:
    image: ${BACKEND_IMAGE:?Set BACKEND_IMAGE to an approved GHCR tag}
    pull_policy: always
  frontend:
    image: ${FRONTEND_IMAGE:?Set FRONTEND_IMAGE to an approved GHCR tag}
    pull_policy: always
